steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/badge-260212/native-image', '-f', 'Dockerfile.native-image', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/badge-260212/native-image']

  - name: 'gcr.io/cloud-builders/mvn'
    args: ['-B', '-P', 'native', 'package']
    env:
      - 'COMMIT_SHA=$COMMIT_SHA'
      - 'SHORT_SHA=$SHORT_SHA'
      - 'GITHUB_TOKEN=$_GITHUB_TOKEN'

  - name: 'gcr.io/badge-260212/native-image'
    args: ['./build.sh']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/badge-260212/badge', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'eu.gcr.io/badge-260212/badge']

  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'badge', '--image', 'eu.gcr.io/badge-260212/badge:latest', '--region', 'europe-west4', '--platform', 'managed', '--concurrency', '80', '--max-instances', '10', '--memory', '768Mi', '--timeout', '60s', '--allow-unauthenticated']


options:
  machineType: 'N1_HIGHCPU_8'
